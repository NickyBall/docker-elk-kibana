# ELK Cluster Configuration
# Copy this file to .env on each VM and update with your actual IP addresses
#
# USAGE:
#   cp .env.example .env
#   nano .env  # Edit with your actual IPs
#
# Then start services with:
#   docker-compose -f docker-compose-es-node1.yml up -d

# Elasticsearch Node IP Addresses
# IMPORTANT: Use the VM's actual IP addresses (not Docker internal IPs)
# Replace these with your actual VM IP addresses

# VM1's IP address (es-node1)
ES_NODE1_IP=10.0.1.10

# VM2's IP address (es-node2)
ES_NODE2_IP=10.0.1.11

# VM3's IP address (es-node3)
ES_NODE3_IP=10.0.1.12

# NOTE: All variables must be set on ALL VMs (even if not directly used)
# This ensures proper network.publish_host configuration

# Optional: Elasticsearch JVM Memory Settings
# Adjust based on your VM RAM (use 50% of available RAM)
# ES_JAVA_HEAP=4g
